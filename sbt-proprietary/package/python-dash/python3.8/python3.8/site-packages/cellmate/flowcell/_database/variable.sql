-- name: get^
SELECT
    value
FROM
    variable
WHERE
    name = :name;

-- name: get-all
SELECT
    *
FROM
    variable;

-- name: add-bulk*!
INSERT INTO
    variable (name, value)
VALUES
    (:name, :value);