"""Dict-like (MutableMapping) classes with specialized state management.

E.g., a directory of PID files can be interpreted by a store as a collection of running programs.
In this case, a new PID file added to the directory (even by other processes) will be instantly
reflected in the store as a new running program.
"""
from ._store import Store
from ._dict_store import DictStore
from ._directory_store import DirectoryStore
from ._combined_store import CombinedStore
