"""Defines the DictStore."""
from ._store import Store


class DictStore(dict, Store):
	"""Store with file dict-based state management.

	Being dict-based, this store stores data in memory (and not in the filesystem as e.g. the
	DirectoryStore).
	"""
