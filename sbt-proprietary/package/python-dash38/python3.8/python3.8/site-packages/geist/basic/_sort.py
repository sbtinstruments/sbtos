def sort_by_key(source):
	"""Return a new dict with the same keys and values but in sorted order.

	Note that this is only possible since Python 3.7, where dict insertion
	order is guaranteed to be preserved.
	"""
	return {key: source[key] for key in sorted(source.keys())}
